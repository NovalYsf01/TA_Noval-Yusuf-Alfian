version: '3'

services:
  db_noval:
    volumes:
      - noval_db_data:/var/lib/mysql

  nginx_noval:
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.local.conf:/etc/nginx/conf.d/default.conf

volumes:
  noval_db_data: